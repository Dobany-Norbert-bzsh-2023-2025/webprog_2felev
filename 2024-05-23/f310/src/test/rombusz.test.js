import assert from 'assert';
import {perimeter   Calc} from '../app/rombusz.js'
describe ('Rombusz kerülete',function()
{
    it('30 -> 120?',function(){
        const actual = perimeterCalc(30);
        const expected = 120;
        assert.equal(acutal,expected)
    });
})